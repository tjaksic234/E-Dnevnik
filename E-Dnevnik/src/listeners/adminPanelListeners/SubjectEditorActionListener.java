package listeners.adminPanelListeners;

import java.util.EventListener;

public interface SubjectEditorActionListener extends EventListener {

    void subjectEditorEventOccurred(SubjectEditorEvent event);
}
